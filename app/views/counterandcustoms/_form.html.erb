<%= form_for(counterandcustom) do |f| %>
  <% if counterandcustom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(counterandcustom.errors.count, "error") %> prohibited this counterandcustom from being saved:</h2>

      <ul>
      <% counterandcustom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :year %>
    <%= f.number_field :year %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :report_type %>
    <%= f.text_field :report_type %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
