<%= form_for(source_report) do |f| %>
  <% if source_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(source_report.errors.count, "error") %> prohibited this source_report from being saved:</h2>

      <ul>
      <% source_report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reportname %>
    <%= f.text_field :reportname %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :year %>
    <%= f.number_field :year %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
